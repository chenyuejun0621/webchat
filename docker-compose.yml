services:
  webchat:
    # 从当前目录的 Dockerfile 构建
    build: .
    container_name: webchat
    ports:
      - "8000:8000"
    # 从 .env 文件读取环境变量（保持密钥安全）
    env_file:
      - .env
    # 将数据库文件映射到宿主机，防止容器删除后数据丢失
    volumes:
      - ./data:/app/data
    restart: unless-stopped
