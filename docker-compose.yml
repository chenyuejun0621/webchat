services:
  webchat:
    # 从当前目录的 Dockerfile 构建
    build: .
    container_name: webchat
    ports:
      - "8000:8000"
    # 从 .env 文件读取环境变量（保持密钥安全）
    env_file:
      - .env
    # 容器退出自动重启
    restart: unless-stopped
